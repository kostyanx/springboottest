
CREATE TABLE CONTACT (
	CONTACT_ID BIGINT GENERATED BY DEFAULT AS IDENTITY,
	FIRST_NAME varchar(255) not null,
	LAST_NAME varchar(255) not null
);

insert into CONTACT (FIRST_NAME, LAST_NAME) values ('Dave', 'Syer');
